'use strict';

const { expect } = require('./resources/chai');

describe('Decorator', () => {


    describe('test', () => {
        it('should return true', () =>
            expect(true).to.be.true,
        );
    });
});
